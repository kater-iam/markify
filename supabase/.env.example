# Supabaseローカル環境の設定
# これらの値はsupabase CLIによって生成される固定値です
# `supabase status --output json | jq -r '.api.service_role_key'` で取得可能
SUPABASE_URL=http://127.0.0.1:54411
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# シード画像の設定
# Supabaseストレージのバケット名（固定値）
SEED_IMAGES_BUCKET=original_images 